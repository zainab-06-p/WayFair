# Production API URL — replace with your actual backend URL when deployed
# If running backend locally with ngrok, use the ngrok URL here
REACT_APP_API_URL=http://localhost:5000
REACT_APP_SOCKET_URL=http://localhost:5000

# Do NOT add secrets here — this file is committed to git
# Add actual production values in Vercel dashboard → Settings → Environment Variables
